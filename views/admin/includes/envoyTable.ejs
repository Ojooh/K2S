<div class="row listn <%= tab.row_list%>">
    <div class="col-12">
        <div class="card card-stats mt-5">
            <div class="card-table-title bg-success">
            <h4 class="card-title ">Envoys Table</h4>
            <p class="card-description"> List of envoys for the Kids To school Foundation.</p>

            </div>
            <div class="card-body mt-5">
                <% if (envs.length> 0) {%>
                    <div class="table-responsive">
                        <table class="table table-hover table-striped">
                            <thead>
                                <tr>
                                    <th scope="col">S/N</th>
                                    <th scope="col">Name</th>
                                    <th scope="col">Proffession</th>
                                    <th scope="col">Kids</th>
                                    <th scope="col">Date Created</th>
                                    <th scope="col">Last Login</th>
                                    <th scope="col">Status</th>
                                    <th scope="col">Action</th>
                                </tr>
                            </thead>
                        <% if (envs.length> 1) { %>
                            <tbody>
                            <% for (var i=0; i < envs.length; i++) { %>
                                <tr>
                                    <th scope="row">
                                        <%= i + 1; %>
                                    </th>
                                    <td>
                                        <%= envs[i].fname + " " + envs[i].lname %>
                                    </td>
                                    <td>
                                        <%= envs[i].proffession %>
                                    </td>
                                    <td>
                                        0
                                    </td>
                                    <td class="pretty-date">
                                        <%= envs[i].date_created %>
                                    </td>
                                    <td class="pretty-date">
                                        <%= envs[i].last_login %>
                                    </td>
                                <% if (envs[i].id !=user.id) { %>
                                    <td>
                                    <% if (envs[i].is_active) { %>
                                        <div class="custom-control custom-switch">
                                            <input type="checkbox" class="custom-control-input" id="customSwitch-<%= envs[i].id %>" value="False"
                                                checked>
                                            <label class="custom-control-label" for="customSwitch-<%= envs[i].id %>"> Active</label>
                                        </div>
                                    <% } else { %>
                                        <div class="custom-control custom-switch">
                                            <input type="checkbox" class="custom-control-input" id="customSwitch-<%= envs[i].id %>"
                                                value="True">
                                            <label class="custom-control-label" for="customSwitch-<%= envs[i].id %>"> In-Active</label>
                                        </div>
                                    <% } %>
                                    </td>
                                <% } else { %>
                                    <td></td>
                                <% } %>
                                    <td class="td-actions">
                                        <a href="#" class="profile text-info mr-4" data-id="<%= envs[i].id %>"
                                            data-url="/admin/User/get_profile" data-type="display-modal" data-toggle="tooltip"
                                            data-placement="top" title="Envoy Details">
                                            <i class="fas fa-info-circle"></i>
                                        </a>
                                        <a href="#" class="edit-envoy text-primary mr-4" data-id="<%= envs[i].id %>"
                                            data-url="/admin/User/get_profile" data-type="edit-modal" data-toggle="tooltip" data-placement="top"
                                            title="Edit Envoy">
                                            <i class="fas fa-pencil-alt"></i>
                                        </a>
                                        <% if (user.user_type=="ADMS" ) { %>
                                            <a href="#" class="delete-envoy text-danger" data-id="<%= envs[i].id %>"
                                                data-url="/admin/User/delete_profile" data-toggle="tooltip" data-placement="top"
                                                title="Remove Envoy">
                                                <i class="fas fa-times"></i>
                                            </a>
                                            <% } %>
                        
                                    </td>
                                </tr>
                            <% } %>
                            </tbody>
                        <% } else if (envs.length == 1) { %>
                            <tbody>
                            <% for (var i=0; i < envs[0].length; i++) { %>
                                <tr>
                                    <th scope="row">
                                        <%= i + 1; %>
                                    </th>
                                    <td>
                                        <%= envs[0][i].fname + " " + envs[0][i].lname %>
                                    </td>
                                    <td>
                                        <%= envs[0][i].proffession %>
                                    </td>
                                    <td>
                                        0
                                    </td>
                                    <td class="pretty-date">
                                        <%= envs[0][i].date_created %>
                                    </td>
                                    <td class="pretty-date">
                                        <%= envs[0][i].last_login %>
                                    </td>
                                    <% if (envs[0][i].id !=user.id) { %>
                                        <td>
                                            <% if (envs[0][i].is_active) { %>
                                                <div class="custom-control custom-switch">
                                                    <input type="checkbox" class="custom-control-input" id="customSwitch-<%= envs[0][i].id %>"
                                                        value="False" checked>
                                                    <label class="custom-control-label" for="customSwitch-<%= envs[0][i].id %>"> Active</label>
                                                </div>
                                                <% } else { %>
                                                    <div class="custom-control custom-switch">
                                                        <input type="checkbox" class="custom-control-input" id="customSwitch-<%= envs[0][i].id %>"
                                                            value="True">
                                                        <label class="custom-control-label" for="customSwitch-<%= envs[0][i].id %>"> In-Active</label>
                                                    </div>
                                                    <% } %>
                                        </td>
                                        <% } else { %>
                                            <td></td>
                                            <% } %>
                                                <td class="td-actions">
                                                    <a href="#" class="profile text-info mr-4" data-id="<%= envs[0][i].id %>"
                                                        data-url="/admin/User/get_profile" data-type="display-modal" data-toggle="tooltip"
                                                        data-placement="top" title="Envoy Details">
                                                        <i class="fas fa-info-circle"></i>
                                                    </a>
                                                    <a href="#" class="editenvoyr text-primary mr-4" data-id="<%= envs[0][i].id %>"
                                                        data-url="/admin/User/get_profile" data-type="edit-modal" data-toggle="tooltip"
                                                        data-placement="top" title="Edit Envoy">
                                                        <i class="fas fa-pencil-alt"></i>
                                                    </a>
                                                    <% if (user.user_type=="ADMS" ) { %>
                                                        <a href="#" class="delete-envoy text-danger" data-id="<%= envs[0][i].id %>"
                                                            data-url="/admin/User/delete_profile" data-toggle="tooltip" data-placement="top"
                                                            title="Remove Envoy">
                                                            <i class="fas fa-times"></i>
                                                        </a>
                                                        <% } %>
                                
                                                </td>
                                </tr>
                            <% } %>
                            </tbody>
                        <% } %>

                        </table>
                    </div>
                <% } else { %>
                    <div class="my-5">
                        <h6 class="text-center text-danger">No Envoys Available</h6>
                    </div>
                <% } %>
            </div>
        </div>
    </div>
</div>
<!-- Kids Table End -->